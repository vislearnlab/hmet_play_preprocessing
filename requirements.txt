# Runtime dependencies
#
# Current scripts (`convert_fps.py`, `sync_av.py`) need:
#   1. Python 3.9+ (standard library only)
#   2. System ffmpeg and ffprobe on PATH
#
# Lab default is conda (environment.yml + ./setup.sh):
#   conda activate hmet-preprocess
#
# No pip packages are required today. When a future tool adds Python deps
# (e.g. Neon CSV helpers, audio correlation), pin them below and in
# environment.yml, then re-run ./setup.sh:
#
#   package==1.2.3
